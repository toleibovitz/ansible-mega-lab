# ansible-mega-lab

1. This lab will provision the network topology via the EVE-NG SDK
2. It will them login into a Linux Node with the topology, pull a startup script, clone a repo and push the ansible configs to each host
3. After that it will then run another script to automate the back up configs.
